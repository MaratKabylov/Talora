# Supabase Database Types

This directory is reserved for generated Supabase database types.

Expected generated file:

```text
types/database.types.ts
```

Generate this file after the migrations have been applied to a local or linked
Supabase project. Do not maintain table definitions manually here: the SQL
migrations remain the source of truth.
